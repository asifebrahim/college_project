spring.application.name=cost-prediction

# Gemini API settings (optional). When not set, service returns a local mock response.
# Set `gemini.api.url` to the HTTP endpoint of your Gemini-compatible inference API.
# Set `gemini.api.key` if the API requires a bearer token.
gemini.api.key=AIzaSyAOcrFfy4kFsyNGlHlh313c0K2_tQTRSGw
# Optional: model name to use when defaulting to Google Generative Language endpoint.
# If you only have an API key and no URL, the service will construct a Google endpoint using
# this model name. Default: gemini-2.5-flash
gemini.model=gemini-2.5-flash
